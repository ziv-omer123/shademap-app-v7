/**
 * Copy this file to config.js and fill in your own key.
 * config.js is git-ignored so you never accidentally commit your key.
 */
const CONFIG = {
  // From your ShadeMap welcome email. Works on http://localhost for now —
  // email api@shademap.app to whitelist a real domain before you deploy.
  SHADEMAP_API_KEY: "YOUR_SHADEMAP_API_KEY_HERE",
};
